import { useEditor } from '@annotator/editor'

/** @public @react */
export function AnnotatorOverlays() {
	// Arrow functionality has been removed
	// This component is kept for API compatibility but no longer renders anything
	return null
}

/** @public @react */
export function AnnotatorArrowHints() {
	// Arrow functionality has been removed
	// This component is kept for API compatibility but no longer renders anything
	return null
}
